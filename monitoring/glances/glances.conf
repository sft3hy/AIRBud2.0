[global]
refresh=2
check_update=false

[influxdb2]
host=influxdb
port=8086
protocol=http
org=smartrag_org
bucket=glances
# Must match DOCKER_INFLUXDB_INIT_ADMIN_TOKEN in docker-compose.yml
token=smartrag-admin-token

[docker]
disable=False
all=True 

[containers]
disable=False