apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "smart-rag.fullname" . }}-glances-conf
data:
  glances.conf: |
    [global]
    check_update=false
    [influxdb2]
    host={{ include "smart-rag.fullname" . }}-influxdb
    port=8086
    protocol=http
    org={{ .Values.influxdb.org }}
    bucket={{ .Values.influxdb.bucket }}
    token={{ .Values.global.secrets.influxToken }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "smart-rag.fullname" . }}-monitor
  labels:
    {{- include "smart-rag.labels" . | nindent 4 }}
    component: monitor
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "smart-rag.selectorLabels" . | nindent 6 }}
      component: monitor
  template:
    metadata:
      labels:
        {{- include "smart-rag.selectorLabels" . | nindent 8 }}
        component: monitor
    spec:
      # Required to monitor Host processes
      hostPID: true 
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: glances
          image: nicolargo/glances:latest-full
          command: 
            - /bin/sh
            - -c 
            - "glances -C /glances/conf/glances.conf -q --export influxdb2 --debug"
          env:
            - name: DOCKER_HOST
              value: "unix://var/run/docker.sock"
            - name: PYTHONUNBUFFERED
              value: "1"
          volumeMounts:
            - name: docker-sock
              mountPath: /var/run/docker.sock
              readOnly: true
            - name: config
              mountPath: /glances/conf/glances.conf
              subPath: glances.conf
      volumes:
        - name: docker-sock
          hostPath:
            path: /var/run/docker.sock
        - name: config
          configMap:
            name: {{ include "smart-rag.fullname" . }}-glances-conf
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}